apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: backend-go-payment-np
  namespace: backend-development
spec:
  podSelector:
    matchLabels:
      app: backend-go-payment
  policyTypes:
  - Ingress
