apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Infrastructure platform services — di-manage ArgoCD via infrastructure Application.
resources:
  - progressive-delivery/install
  - gateway-api
